================================================================================
================================================================================

        WEBMARS — ENHANCEMENT PLAN EXECUTION MASTER PROMPT (V1)
          Full PDF Plan Ingestion + Complete Implementation + PR Merge
              PERSISTENT SELF-REFERENCING EXECUTION DOCUMENT

================================================================================
================================================================================

DOCUMENT VERSION: 1.0
PREDECESSOR: none (first master prompt for the WebMARS repository)
SOURCE PLAN: C:\Users\Owner\Downloads\WebMARS-Enhancement-Plan.pdf
TARGET REPOSITORY: https://github.com/Webmarssimulator/WebMARS.git
EXECUTION ENVIRONMENT: Claude Code (Fable 5) running inside PowerShell on
  Windows. All commands in this document are PowerShell-compatible.
CURRENT APP STATE: see AGENT 00 ground truth in Appendix A-00.
PURPOSE: Read the WebMARS-Enhancement-Plan.pdf END TO END, extract EVERY
  SINGLE requirement, enhancement, fix, and detail it contains — every single
  one — turn each into a tracked agent in this document, and implement all of
  them until the ENTIRE plan is done, fully working, with evidence. Every
  agent's work is committed on a branch, pushed, opened as a pull request,
  and MERGED TO MAIN before moving on.
TOTAL AGENTS: 18 — fixed agents 00, 01, 90, 91, 92, 93, 94 + dynamic
  implementation agents 02..12 generated by AGENT 01 from the PDF
  (48 requirements extracted; see Appendix B).
TARGET END STATE: 100% of the requirements in WebMARS-Enhancement-Plan.pdf
  implemented and verified with evidence, every requirement traceable in the
  REQUIREMENTS TRACEABILITY MATRIX (Appendix B) to a merged PR, the project
  building cleanly, all work merged to main via pull requests, the AI tooling
  disclosure landed in its own PR, and the FINAL VERIFICATION agent passed.

THE ONLY ACCEPTABLE STOPPING CONDITION: every agent in the PROGRESS LEDGER is
✅ COMPLETE and AGENT 94 (FINAL VERIFICATION) has passed. Not before. If you
are reading this mid-session, go to the GOLDEN LOOP below and resume.

================================================================================
                    THE EXECUTION PROTOCOL — READ THIS FIRST
================================================================================

This document is your single source of truth. You execute it agent by agent,
returning to this document after EVERY agent, until everything is finished.
You do not stop when one agent is done. You do not stop when most agents are
done. You stop when ALL agents are done and final verification has passed.

THE GOLDEN LOOP — FOLLOW THIS EXACTLY:

  STEP 1.  Open this master document. Find the PROGRESS LEDGER.
  STEP 2.  Identify the FIRST agent whose status is NOT "✅ COMPLETE".
  STEP 3.  Read that agent's full section, top to bottom, before touching code.
  STEP 4.  Execute EVERY task in that agent, in order. No task is optional.
  STEP 5.  Run the agent's ACCEPTANCE CRITERIA. Every checkbox must pass with
           real evidence (file:line, command output, build result, described
           manual test). "It looks right" is NEVER evidence.
  STEP 6.  If ANY criterion fails, fix the failure and re-run STEP 5. Do not
           proceed with a failing criterion. Do not soften a criterion to make
           it pass.
  STEP 7.  Update the PROGRESS LEDGER (status + dated ledger note) and fill in
           the agent's Appendix A verification block IN THIS FILE.
  STEP 8.  Commit on the agent's branch, push, open the PR, MERGE IT TO MAIN,
           confirm remote main updated, using the GIT + PR WORKFLOW below and
           the agent's specified commit message.
  STEP 9.  RETURN TO STEP 1. Immediately. You do NOT stop and wait for
           permission to continue. Begin the next incomplete agent.
  STEP 10. When ALL agents 00..NN and 90..93 are ✅ COMPLETE, execute AGENT 94
           (FINAL VERIFICATION). Only a passing AGENT 94 ends the loop.

RESUME BEHAVIOR: If the session is interrupted, context is lost, or you are
restarted for any reason — re-open this document, re-read the PROGRESS LEDGER
and the Appendix A blocks that are already filled, and resume at the FIRST
incomplete agent.

CRITICAL RULES — THESE OVERRIDE CONVENIENCE, ALWAYS

  R1.  NEVER skip an agent. NEVER reorder agents. The ledger order is the
       execution order.
  R2.  NEVER mark an agent ✅ COMPLETE without evidence recorded in its
       Appendix A block.
  R3.  NEVER ship mock, placeholder, or hardcoded sample data in production
       paths. Every async surface gets real loading / empty / error states.
  R4.  ALWAYS build (and run tests) before declaring an agent complete.
  R5.  ALWAYS merge to main through a pull request. One branch and one PR per
       agent. Confirm MERGED before returning to the loop.
  R6.  NEVER `git push --force` to main. Only `--force-with-lease` on a
       feature branch created this session, with explicit owner approval.
  R7.  GIT IDENTITY IS THE OWNER'S. Every commit is authored by the git
       identity resolved from `git config user.name` / `git config user.email`
       — Bryan Djenabia + his configured email. NEVER pass `--author`. NEVER
       set GIT_COMMITTER_NAME/EMAIL. NEVER add a Co-Authored-By trailer
       naming Claude, Anthropic, or any AI. Disclosure is AGENT 93's job.
  R8.  VERIFY IDENTITY BEFORE THE FIRST COMMIT of the session. Wrong identity
       = STOP and surface to owner.
  R9.  WATCH FOR IDENTITY DRIFT. Any non-owner author/committer appearing =
       STOP and surface before doing anything else.
  R10. THE PDF IS THE SPEC. Every REQ ends IMPLEMENTED with a merged PR ref,
       or ⛔ BLOCKED with a precise owner action. No third state.
  R11. Genuinely owner-only actions → ⛔ BLOCKED with the EXACT step, then
       continue the loop.
  R12. NEVER degrade quality to hit a checkbox. Conflicts between the plan
       and correctness resolve toward correctness, recorded honestly.
  R13. Frontend work at senior standards: design tokens, WCAG AA, keyboard
       navigable, responsive, intentional loading/empty/error/hover states.
  R14. Security is non-negotiable: sanitize inputs, no secrets, no injection,
       dependencies audited when touched.
  R15. This document is under version control. Every ledger/appendix update
       is committed as part of that agent's PR.

GIT + PR WORKFLOW — ONE BRANCH, ONE PR, ONE MERGE PER AGENT

  git checkout main ; git pull origin main
  git checkout -b <type>/agent-NN-<short-name>
  # work
  git add -A ; git commit -m "<agent's commit message>"
  git push -u origin <branch>
  gh pr create --base main --head <branch> --title "<title>" --body "<body>"
  gh pr merge --squash --delete-branch
  git checkout main ; git pull origin main
  gh pr view <n> --json state,mergedAt   # must be MERGED

PDF COVERAGE RULES: P1 read every page; P2 permanent REQ IDs in reading
order, never renumbered; P3 every REQ in Appendix B with source page,
summary, category, owner, status, PR, evidence; P4 every REQ owned by
exactly one dynamic agent; P5 ambiguity resolves to the MORE complete
interpretation, recorded; P6 AGENT 91 re-reads the PDF cold (Q1/Q2/Q3).

INFRASTRUCTURE VIA CLI: schema changes as migrations via CLI; payments via
Stripe CLI; secrets via secrets CLI, never committed; deployment as code.
If none apply, AGENT 92 records that finding and completes trivially.

SESSION START CHECKLIST: S1 open this doc; S2 read ledger notes bottom-up;
S3 read last two Appendix A blocks (stricter record wins); S4 git checkout
main + pull + status (dirty tree = investigate); S5 identity check; S6 gh
auth status; S7 enter the GOLDEN LOOP.

CONTEXT RECOVERY: CR1 this doc; CR2 gh pr list --state merged; CR3 git log
main; CR4 working tree; CR5 resume only when all agree. Disagreement rule:
merged PRs > main history > this doc > memory. Fix the doc to match reality.

OWNER INTERACTION: surface immediately for identity issues, unreadable PDF,
force-push needs, destructive out-of-scope actions. Surface-and-continue
(⛔) for owner-only actions (gh auth login, live secrets, required human
reviews). Never surface for permission to continue, progress summaries, or
decisions this doc already makes. Format: what happened → why it blocks →
exact owner action → what you're doing meanwhile.

PER-AGENT CHECKLIST: first incomplete agent; section read fully; branch off
fresh main; every task in order; every criterion evidenced; build+tests
green NOW; Appendix A filled; Appendix B flipped; ledger line + note; doc
committed with the work; PR merged + verified; return to STEP 1.

================================================================================
                              PROGRESS LEDGER
================================================================================

Mark each agent: ⬜ NOT STARTED | 🔄 IN PROGRESS | ✅ COMPLETE | ⛔ BLOCKED

  AGENT 00 — Environment, Clone & Identity Pre-Flight ........... ✅ COMPLETE
  AGENT 01 — PDF Ingestion & Requirements Extraction ............ ✅ COMPLETE

  ── DYNAMIC IMPLEMENTATION AGENTS (inserted by AGENT 01) ──────────────────────
  AGENT 02 — API Client, Toast System & Env-Aware Base URL ...... ✅ COMPLETE
  AGENT 03 — Editor Persistence Across Refresh .................. ✅ COMPLETE
  AGENT 04 — Monaco Light Theme + System Preference ............. ✅ COMPLETE
  AGENT 05 — rem Pseudo-Instruction (Assembler + Tests) ......... ⬜ NOT STARTED
  AGENT 06 — Tools: Bitmap Grid Sizes + Screen Magnifier ........ ⬜ NOT STARTED
  AGENT 07 — Accounts UI: Auth Slice + AuthModal + Toolbar ...... ⬜ NOT STARTED
  AGENT 08 — Save, Share & Deep Links (?snippet=<id>) ........... ⬜ NOT STARTED
  AGENT 09 — My Snippets Drawer + Public Feed ................... ⬜ NOT STARTED
  AGENT 10 — Run History: Logging Hook + HistoryPanel ........... ⬜ NOT STARTED
  AGENT 11 — Polish, A11y, Docs, Tests Completion, v1.3 Issues .. ⬜ NOT STARTED
  AGENT 12 — Backend: Verification + Gap PR from Fork ........... ⬜ NOT STARTED
  ──────────────────────────────────────────────────────────────────────────────

  AGENT 90 — Cross-Cutting Quality Sweep ........................ ⬜ NOT STARTED
  AGENT 91 — Gap Closure: PDF vs Reality, Q1/Q2/Q3 .............. ⬜ NOT STARTED
  AGENT 92 — Infrastructure & Config via CLI .................... ⬜ NOT STARTED
  AGENT 93 — AI Tooling Disclosure (single-purpose PR) .......... ⬜ NOT STARTED
  AGENT 94 — FINAL VERIFICATION + FULL REGRESSION + MERGE ....... ⬜ NOT STARTED

  LEDGER NOTES (append one dated line per completed agent):
  ------------------------------------------------------------------------------
  [2026-07-10] AGENT 00: pre-flight complete. Toolchain: git 2.54.0.windows.1,
  gh 2.92.0 (auth: BryanD17, keyring), node v24.14.0, PowerShell 5.1.26100.
  Cloned to C:\Users\Owner\dev\WebMARS; default branch main. Identity:
  user.name="Bryan Djenabia", user.email="bdjenabia5@gmail.com" (owner —
  verified). PDF: 822,217 bytes, 60 pages, text-extractable, copied to
  docs/. Ground truth: WebMARS is a web-based MIPS assembly simulator
  (MARS-style) — React 19 + TypeScript 6 + Vite 8 + Tailwind 4 + Zustand 5 +
  Monaco editor. Build: `npm install` then `npm run build` (tsc -b && vite
  build) → exit 0, main bundle 417.64 kB (116.94 kB gzip), CSS 68.61 kB.
  Tests: `npm test` (vitest) → 14 files, 125/125 passed. Lint: eslint 10
  (eslint.config.js). CI: .github/workflows/ci.yml. Deploy: vercel.json +
  docs/VERCEL_DEPLOY.md. Existing docs overlapping the plan: docs/PRD.md,
  STRETCH_ROADMAP.md, FINAL_REPORT.md, TASKS_LANDON.md, TASKS_ZACHARY.md,
  DEMO_SCRIPT.md. Baseline npm audit: 8 vulnerabilities (3 low, 2 moderate,
  1 high, 2 critical) — pre-existing, routed to AGENT 90/92. Src layout:
  src/core (assembler, simulator, memory, registers, syscalls, instructions),
  src/ui (~35 components + tokens.css), src/landing, src/hooks, src/lib,
  src/tests. PR #40 merged, main @ 4c9d661.
  ------------------------------------------------------------------------------
  [2026-07-10] AGENT 01: PDF ingested 60/60 pages; 48 REQs extracted into
  Appendix B; 11 dynamic agents (02–12) generated. Major scoping findings
  (SF1–SF6): plan spans two repos; backend v1.2 largely pre-implemented
  upstream by landclay715 (verified file-by-file); backend repo is
  READ-only for BryanD17 → gap work ships as PR from fork; backend LIVE at
  https://webmars-api-production.up.railway.app (200 verified); frontend
  live on Vercel without integration; no Docker/Postgres locally →
  Testcontainers suite authored but not locally executable. G10 ambiguity
  flags: REQ-001/002/014/015/017. PR #41 merged, main @ fff16a8.
  ------------------------------------------------------------------------------
  [2026-07-10] AGENT 02: API client + toasts + env base (REQ-007/018/020)
  done. src/lib/api.ts typed against the REAL upstream serialization (owner
  ={username}, MostRun.snippetTitle, Page envelope — plan skeleton
  corrected); sonner Toaster at App root, theme-aware; .env.local.example.
  5 new api tests; 130/130 green; typecheck/lint/build exit 0; bundle
  450.46 kB (+33 kB sonner). Note: upstream RunController ALREADY has the
  audit-C2 visibility check — AGENT 12 scope shrinks to PUT endpoint +
  Testcontainers. PR #42 merged, main @ 4ac89d2.
  ------------------------------------------------------------------------------
  [2026-07-10] AGENT 03: editor persistence (REQ-001) done. Workspace
  (files + active tab) persists to webmars:workspace-v1, debounced 250ms
  with beforeunload flush; hydrates on boot; simulator state provably
  excluded (payload keys asserted in test). 4 new tests; 134/134 green;
  typecheck/lint/build exit 0. PR #43 merged, main @ e8c6a68.
  ------------------------------------------------------------------------------
  [2026-07-10] AGENT 04: Monaco light theme + system preference (REQ-002)
  done. webmars-light registered; theme prop derived via resolveTheme;
  'system' option follows matchMedia live; SettingsDialog copy fixed.
  LIVE browser proof: editor bg rgb(10,10,10)→rgb(255,255,255)→back on
  radio toggles (playwright + Edge headless). 134/134 green. PR #44.
  ------------------------------------------------------------------------------

================================================================================
          AGENT 00 — ENVIRONMENT, CLONE & IDENTITY PRE-FLIGHT
================================================================================
Executed 2026-07-10. See Appendix A-00 for the full verification block and
the ledger note above for ground truth. Tasks 0.1–0.8 completed as specified
in the source master prompt (toolchain verify, clone, default-branch check,
identity verify, PDF verify + copy, ground truth, master doc landed, PR).

================================================================================
        AGENT 01 — PDF INGESTION & REQUIREMENTS EXTRACTION
================================================================================
PRIORITY: CRITICAL (this agent creates the rest of the plan)

TASKS: 1.1 read the PDF end to end (60 pages). 1.2 extract EVERY actionable
item. 1.3 assign REQ-001.. in reading order; record in Appendix B with page,
summary, category, owner, status. 1.4 completeness check against the PDF's
section list; record total REQ count. 1.5 cluster REQs into dynamic agents
(3–10 REQs each, dependency-ordered). 1.6 generate each agent section with
concrete tasks, acceptance criteria (one checkbox per REQ), commit message;
insert ledger lines + Appendix A blocks. 1.7 update TOTAL AGENTS. 1.8 commit
expanded doc on docs/agent-01-plan-extraction, PR, merge.

ACCEPTANCE: every page read; Appendix B complete with total count; every
section mapped or annotated no-action; every REQ owned; no placeholder text;
ledger + appendices inserted; PR merged.

COMMIT MESSAGE: agent-01-v1: extract full Enhancement Plan into REQ matrix +
dynamic agents

================================================================================
   SCOPING FACTS ESTABLISHED BY AGENT 01 (bind all dynamic agents)
================================================================================
  SF1. The plan spans TWO repos. Frontend = this repo (full write access).
       Backend = landclay715/webmars-api — BryanD17 has READ permission only.
       Backend gaps are delivered as a PR from a fork (AGENT 12); merging
       that PR is Landon's action (⛔ where merge is required).
  SF2. The backend v1.2 work was ALREADY IMPLEMENTED upstream between the
       plan date (2026-05-14) and now (verified 2026-07-10 against
       webmars-api@main): env-var secrets + JwtUtil validation + 8h expiry,
       401 login, CORS w/ prod origins, Flyway V1–V4, validation DTOs,
       rate limiting (hand-rolled, not Bucket4j), owner-checked DELETE,
       visibility-gated GET, /mine, /public, /save, RunController,
       GlobalExceptionHandler, .env.example (.env untracked), README
       rewritten. GAPS upstream: PUT /snippets/{id} (update), Testcontainers
       integration test suite, response DTOs (entities returned directly).
  SF3. The backend is LIVE at https://webmars-api-production.up.railway.app
       (Railway, not Render — the team substituted providers; verified 200
       on GET /snippets/public). The frontend is live at webmarsimulator.com
       (Vercel) WITHOUT any v1.2 integration.
  SF4. Local machine has Java 21 but NO Docker and NO PostgreSQL:
       Testcontainers tests can be authored + compiled but not executed
       locally. Honest disposition required (R2).
  SF5. Frontend already has a theme system: ThemeName = 'dark'|'light'|'hc'
       via data-theme attribute + SettingsDialog radio group; light mode
       deliberately leaves Monaco dark (the bug). The store persists many
       slices manually per-key to localStorage; `source` is not persisted.
       Codebase conventions (data-theme attr, manual persistence keys) win
       over the plan's literal mechanisms where they conflict (Appendix C).
  SF6. VITE_API_BASE production value: https://webmars-api-production.up.railway.app

================================================================================
          AGENT 02 — API CLIENT, TOAST SYSTEM & ENV-AWARE BASE URL
================================================================================
PRIORITY: CRITICAL (every account feature depends on it)
OWNS REQUIREMENTS: REQ-007, REQ-018, REQ-020

CONTEXT
  Foundation layer for all v1.2 integration: the typed HTTP client
  (src/lib/api.ts), the toast notification system (sonner + Toaster at the
  root in src/App.tsx), and environment-aware API base URL with
  .env.local.example. No raw fetch anywhere else in src/ from here on.

TASKS
  02.1 [REQ-007] npm install sonner; mount <Toaster/> near the root in
       src/App.tsx with theme wired to the app's data-theme state.
  02.2 [REQ-020] Create src/lib/api.ts per the plan skeleton (p.30-32):
       ApiError(status, body), authHeader() reading webmars.token, generic
       request<T> with JSON headers + 204 handling, functions: register,
       login, getSnippet, getMySnippets, getPublicSnippets, saveSnippet,
       updateSnippet, deleteSnippet, logRun, getRecentRuns,
       getMostRunPrograms, with the plan's TypeScript interfaces.
  02.3 [REQ-018] BASE = import.meta.env.VITE_API_BASE ?? 'http://localhost:8080';
       add .env.local.example with VITE_API_BASE, documented in README later.
  02.4 [REQ-020] Add src/tests/api.test.ts: auth header attached when token
       exists, ApiError thrown with status on non-2xx (mock fetch).
ACCEPTANCE: all three REQs evidenced; build+lint+typecheck+tests green.
COMMIT MESSAGE: agent-02-v1: typed API client, sonner toasts, env-aware base URL

================================================================================
          AGENT 03 — EDITOR PERSISTENCE ACROSS REFRESH
================================================================================
PRIORITY: CRITICAL (loudest user complaint)
OWNS REQUIREMENTS: REQ-001

CONTEXT
  src/hooks/useSimulator.ts persists number base, layout, theme, fonts,
  breakpoints per-key to localStorage but NOT the editor source. Fix so
  source/files/activeFileId (+ currentSnippetId metadata once it exists)
  survive refresh while simulator state (registers, memory, PC) resets.
  Mechanism follows the store's existing manual-persistence convention OR
  zustand persist middleware — whichever fits the 1592-line store best;
  behavioral acceptance below is what binds. Private-mode localStorage
  failure must not crash (existing try/catch convention).

TASKS
  03.1 [REQ-001] Persist the editor content slice(s) on change; hydrate on
       store creation. Allowlist only: source/files/activeFileId (+ snippet
       metadata when AGENT 08 adds it). Never persist registers, memory,
       console output, PC.
  03.2 [REQ-001] Add src/tests/persist.test.ts: editing source writes the
       storage key; a fresh store rehydrates source; simulator state absent
       from the persisted payload.
ACCEPTANCE: edit → refresh → code preserved; run → refresh → registers
zero; private-mode fallback safe; tests green.
COMMIT MESSAGE: agent-03-v1: persist editor source across refresh

================================================================================
          AGENT 04 — MONACO LIGHT THEME + SYSTEM PREFERENCE
================================================================================
PRIORITY: HIGH
OWNS REQUIREMENTS: REQ-002

CONTEXT
  src/ui/CodeEditor.tsx:220 hardcodes theme="webmars-dark". The app already
  has ThemeName 'dark'|'light'|'hc' via data-theme. Deliver: webmars-light
  Monaco theme (plan token colors p.6-7), editor follows the app theme, a
  'system' option added to the theme picker, live OS-preference tracking.
  [Interpretation per P5: 'hc' keeps webmars-dark (high-contrast shell);
  SettingsDialog copy updated to stop claiming the editor stays dark.]

TASKS
  04.1 [REQ-002] Define webmars-light beside webmars-dark (mipsLanguage.ts
       or wherever defineTheme lives) with the plan's token colors.
  04.2 [REQ-002] Add 'system' to THEMES + SettingsDialog radio group; add
       useSystemColorScheme hook (matchMedia listener); derive effective
       data-theme and Monaco theme; update THEME_LABELS copy.
  04.3 [REQ-002] Persist preference (existing THEME_STORAGE_KEY path).
ACCEPTANCE: light mode lightens the editor immediately; dark returns;
system follows OS live; persists across refresh; build+tests green.
COMMIT MESSAGE: agent-04-v1: Monaco light theme + system theme preference

================================================================================
          AGENT 05 — rem PSEUDO-INSTRUCTION
================================================================================
PRIORITY: HIGH
OWNS REQUIREMENTS: REQ-023

CONTEXT
  rem $rd, $rs, $rt = remainder; expands to div $rs, $rt + mfhi $rd,
  mirroring the existing mul pattern. Files: src/core/assembler/lexer.ts
  (MNEMONICS), parser.ts (OPERAND_SIGNATURES rem: ["R","R","R"]),
  assembler.ts (expansion case). Tests in src/tests/ + HelpDialog entry.

TASKS
  05.1 [REQ-023] Add rem to lexer MNEMONICS, parser signatures, assembler
       expansion (div encoding funct=011010, mfhi funct=010000).
  05.2 [REQ-023] Tests: expansion produces two words; positive remainder;
       negative dividend; rem-by-zero passes through (matches existing div
       runtime behavior). Use the existing golden/instruction harness.
  05.3 [REQ-023] HelpDialog.tsx pseudo-instruction section gets the rem row.
ACCEPTANCE: all rem tests pass; full suite green; help entry present.
COMMIT MESSAGE: agent-05-v1: add rem pseudo-instruction with tests and docs

================================================================================
          AGENT 06 — TOOLS: BITMAP GRID SIZES + SCREEN MAGNIFIER
================================================================================
PRIORITY: MEDIUM
OWNS REQUIREMENTS: REQ-006, REQ-028, REQ-029

CONTEXT
  BitmapDisplay.tsx:16 lacks 8/16 grid options. ScreenMagnifier.tsx:79-83 is
  a placeholder. Deliver the DOM-clone loupe per plan §8.2 (240x160, 2x,
  re-clone only on target change, hint state, Escape, pointerEvents none)
  and data-magnify-region tags on the panels listed p.47.

TASKS
  06.1 [REQ-028] DIMENSION_OPTIONS = [8, 16, 32, 64, 128, 256]; verify 8x8
       and 16x16 render row-major correctly; existing sizes unaffected.
  06.2 [REQ-029] Add data-magnify-region to RegisterTable, MemoryPanel,
       ConsolePanel, StatusBar, FpuRegisterTable + Messages/Symbols/
       Breakpoints panels where small text warrants. Monaco excluded.
  06.3 [REQ-006] Rewrite ScreenMagnifier per the plan component (p.47-48):
       elementFromPoint + closest('[data-magnify-region]'), cloneNode into
       loupe, scale(2) translate centering, hint when no target, Escape
       closes, no per-mousemove re-clone, interactive bits disarmed.
  06.4 [REQ-006] HelpDialog: magnifier description + Monaco limitation +
       OS-zoom pointer; bitmap 8x8/16x16 mention.
ACCEPTANCE: all three REQs evidenced by described hand-tests; build+tests
green; no CPU-spike pattern (clone count vs mousemove verified in code).
COMMIT MESSAGE: agent-06-v1: bitmap 8x8/16x16 + DOM-clone screen magnifier

================================================================================
          AGENT 07 — ACCOUNTS UI: AUTH SLICE + AUTHMODAL + TOOLBAR
================================================================================
PRIORITY: CRITICAL
OWNS REQUIREMENTS: REQ-021

CONTEXT
  Auth slice in useSimulator (authToken/authUsername from webmars.token /
  webmars.username; setAuth/clearAuth), AuthModal (Login/Register tabs,
  busy state, error mapping 401/409/429/network per p.35), toolbar Sign-in
  button ↔ username dropdown with Log out. Style mirrors SettingsDialog
  primitives. Loading states + toasts on every async action (REQ-008
  pattern).

TASKS
  07.1 [REQ-021] Auth slice with localStorage-backed token/username.
  07.2 [REQ-021] AuthModal component per plan sketch; register auto-logs-in;
       focus trap, Escape close, Enter submits (REQ-037 pattern applies now).
  07.3 [REQ-021] Toolbar auth affordance; authenticated state swaps to
       username menu with Log out; mobile shell parity.
ACCEPTANCE: register/login/logout round-trip against the live Railway API
described with results; 401 shows friendly toast; state survives refresh;
build+tests green.
COMMIT MESSAGE: agent-07-v1: account auth slice, AuthModal, toolbar auth UI

================================================================================
          AGENT 08 — SAVE, SHARE & DEEP LINKS
================================================================================
PRIORITY: CRITICAL
OWNS REQUIREMENTS: REQ-022, REQ-009, REQ-038

CONTEXT
  Save-to-Account button (visible when authed), ShareModal after first save
  (copy link, visibility display + toggle → PUT), create-vs-update
  semantics on currentSnippetId, router.ts ?snippet=<id> parse + boot
  loading with 404 toast fallback, popstate restore, document title update.
  NOTE: PUT /snippets/{id} does not exist upstream yet (AGENT 12 PRs it);
  the update path must degrade gracefully (toast on ApiError) until merged.

TASKS
  08.1 [REQ-022] Save flow: POST /snippets/save when currentSnippetId null
       (then ShareModal); PUT when set (no modal); public/private toggle in
       the save dialog defaulting PRIVATE; clipboard copy + toast.
  08.2 [REQ-009] router.ts: getSnippetIdFromUrl/setSnippetIdInUrl; App boot
       effect loads ?snippet=<id>, sets source + snippet metadata + title;
       404 → 'private or does not exist' toast + default example.
  08.3 [REQ-038] popstate: back/forward between /?snippet=N and / restores
       state consistently.
ACCEPTANCE: save→share→incognito-load walk described against live API;
private 404 path verified; back-button behavior verified; build+tests green.
COMMIT MESSAGE: agent-08-v1: snippet save/share modal + deep-link loading

================================================================================
          AGENT 09 — MY SNIPPETS DRAWER + PUBLIC FEED
================================================================================
PRIORITY: HIGH
OWNS REQUIREMENTS: REQ-032, REQ-033

CONTEXT
  MySnippetsDrawer: list /snippets/mine (title, updated, visibility badge),
  click-to-load, edit-in-place title (PUT), delete w/ ConfirmDialog, New
  Snippet button clearing currentSnippetId. PublicFeed: paginated
  /snippets/public browser (title/owner/updated, Prev/Next/page) +
  'See public snippets' link in the drawer's empty state. Empty states per
  the D9 copy (REQ-035 pattern lands with the surface).

TASKS
  09.1 [REQ-032] MySnippetsDrawer with all listed affordances + loading/
       empty/error states.
  09.2 [REQ-033] Public feed surface with pagination + discovery link +
       loading/empty/error states.
ACCEPTANCE: both REQs walked against live API (multiple snippets, both
visibilities); non-owner privacy verified; build+tests green.
COMMIT MESSAGE: agent-09-v1: my-snippets drawer + paginated public feed

================================================================================
          AGENT 10 — RUN HISTORY: LOGGING HOOK + HISTORYPANEL
================================================================================
PRIORITY: HIGH
OWNS REQUIREMENTS: REQ-030, REQ-034

CONTEXT
  Fire-and-forget run logging in useSimulator at run end (COMPLETED/ERROR/
  ABORTED; runStartedAt for duration; skip when unauthenticated or snippet
  unsaved; console.warn on failure, never block the simulator). HistoryPanel
  tab in the right inspector: Recent Runs (status badges, duration,
  timestamp) + Most Run (count, last run), click loads snippet, empty state
  copy per plan.

TASKS
  10.1 [REQ-030] logRunIfPossible helper wired to every run-end path.
  10.2 [REQ-034] HistoryPanel with both sections + loading/empty/error.
ACCEPTANCE: run→history round-trip described against live API; abort and
error paths log correct exitStatus; simulator never blocked (offline test);
build+tests green.
COMMIT MESSAGE: agent-10-v1: run logging hook + history panel

================================================================================
          AGENT 11 — POLISH, A11Y, DOCS, TESTS COMPLETION, v1.3 ISSUES
================================================================================
PRIORITY: HIGH
OWNS REQUIREMENTS: REQ-035, REQ-036, REQ-037, REQ-039, REQ-040, REQ-042,
                   REQ-031, REQ-047, REQ-048

CONTEXT
  The D9 polish pass across every new surface, README rewrite, HelpDialog
  completion check, frontend-test completion check, and the §10.4 backlog
  conversion to GitHub issues.

TASKS
  11.1 [REQ-035] Empty states audit: drawer/history/feed exact copy from
       p.26-27; fix gaps.
  11.2 [REQ-036] ConfirmDialog for destructive deletes with the plan's copy.
  11.3 [REQ-037] Keyboard a11y: AuthModal/ShareModal/ConfirmDialog focus
       trap, Escape, Enter-submit — verified per modal.
  11.4 [REQ-039] Mobile (<768px) usability of all new modals/panels.
  11.5 [REQ-040] README.md rewritten for v1.2 (features, env vars, run +
       deploy steps, API link).
  11.6 [REQ-042] HelpDialog completeness: rem, magnifier, bitmap sizes,
       theme options (added by 05/06/04) — verify + fill gaps.
  11.7 [REQ-031] Frontend test completeness: api.test.ts, persist.test.ts,
       rem tests all present and green (added by 02/03/05) — verify + fill.
  11.8 [REQ-047] Create GitHub issues labeled v1.3 for the ten §10.4
       backlog items (frontend repo; backend-flavored ones reference the
       backend repo in-body).
  11.9 [REQ-048] Demo video/announcement: ⛔ owner action (cannot record
       video) — exact ask recorded in ledger.
ACCEPTANCE: every REQ evidenced (file:line / issue URLs / walkthroughs);
build+lint+typecheck+tests green.
COMMIT MESSAGE: agent-11-v1: v1.2 polish — a11y, empty states, docs, issues

================================================================================
          AGENT 12 — BACKEND: VERIFICATION + GAP PR FROM FORK
================================================================================
PRIORITY: HIGH (merge step is owner/Landon-gated)
OWNS REQUIREMENTS: REQ-003, REQ-004, REQ-005, REQ-010, REQ-011, REQ-012,
                   REQ-013, REQ-014, REQ-015, REQ-016, REQ-024, REQ-025,
                   REQ-026, REQ-027, REQ-041

CONTEXT
  Two halves. (a) VERIFY: most backend REQs pre-exist upstream — record
  file:line evidence per REQ from webmars-api@main. (b) GAP PR: fork
  webmars-api to BryanD17, implement PUT /snippets/{id} + UpdateSnippetRequest
  (REQ-026 gap), the Testcontainers integration test suite (REQ-015 —
  authored + compiling; cannot execute locally, SF4), and the audit-C2
  visibility check on POST /runs (REQ-027 extension); open an upstream PR.
  Merging is Landon's action → that step is ⛔ with the PR URL as the ask.
  Secret rotation + history scrub status (REQ-005): verify what is
  externally verifiable (env indirection, .env untracked, old secret absent
  from current code); production rotation + history rewrite remain owner/
  Landon actions → ⛔ with exact steps.

TASKS
  12.1 Verification sweep with per-REQ evidence (REQ-003/004/010/011/012/
       013/014/016/024/025/027-existing/041).
  12.2 Fork + branch: PUT endpoint w/ ownedOr404, UpdateSnippetRequest DTO,
       partial-update semantics per plan §7.5; visibility check on POST
       /runs per SECURITY_AUDIT C2.
  12.3 Testcontainers: pom additions, PostgresTestBase, AuthControllerTest,
       SnippetControllerTest, RunControllerTest, JwtUtilTest per §8.4/§9.1.
       ./mvnw compile + test-compile locally (execution ⛔ no Docker, SF4).
  12.4 Open upstream PR with full evidence body; record URL; merge = ⛔.
ACCEPTANCE: every owned REQ dispositioned IMPLEMENTED-with-evidence,
PR-open-⛔-merge, or ⛔-owner-action; fork compiles; upstream PR URL logged.
COMMIT MESSAGE: agent-12-v1: backend v1.2 gaps — PUT update, run-log
visibility check, Testcontainers suite (PR from fork)

================================================================================
              AGENT 90 — CROSS-CUTTING QUALITY SWEEP
================================================================================
Runs after all dynamic agents. Sweeps every touched surface for: design-token
consistency (no magic numbers); accessibility (WCAG AA — semantics, keyboard
reachability + visible focus, measured contrast ≥4.5:1 body / ≥3:1 large,
prefers-reduced-motion); responsive at ~375/~768/≥1280 (no horizontal leaks,
touch targets ≥44px); async loading/empty/error states with actionable
errors; performance (bundle size vs the A-00 baseline, render waste, steady
frame time on hot surfaces, no per-frame allocations, asset sizes); security
(secret scan of this run's commits, input validation at touched boundaries,
audit of deps ADDED this run, no eval/dangerous HTML); dead code removal.
Findings are FIXED in this agent. Branch fix/agent-90-quality-sweep.
ACCEPTANCE: all sweeps evidenced (grep results, measured pairs, breakpoints
checked, bundle numbers, scan output); build+tests green; PR merged.
COMMIT MESSAGE: agent-90-v1: cross-cutting quality sweep — tokens, a11y,
responsive, perf, security

================================================================================
        AGENT 91 — GAP CLOSURE: PDF vs REALITY (Q1 / Q2 / Q3)
================================================================================
Adversarial re-check; trusts nothing. Q1: every IMPLEMENTED REQ exercised
end-to-end against main — failures flip back and get fixed now. Q2: per REQ
theme, sweep the whole codebase for other instances the PDF didn't spell
out; fixes recorded as REQ-0xx-EXT-n rows. Q3: cold re-read of all 60 pages
against Appendix B; unmapped actionable sentences become new REQs (fixed
here if small, new dynamic agent before 90 if large — 90 then re-runs).
Also verify every P5 ambiguity took the MORE complete reading. Re-run
build+tests. Branch fix/agent-91-gap-closure.
ACCEPTANCE: Q1/Q2/Q3 evidenced per cluster ("nothing found" names the places
checked); Appendix B exhaustively green or ⛔; build+tests pass; PR merged.
COMMIT MESSAGE: agent-91-v1: gap closure — Q1/Q2/Q3 sweep of Enhancement
Plan vs main

================================================================================
          AGENT 92 — INFRASTRUCTURE & CONFIG VIA CLI
================================================================================
Owns every INFRA-category REQ plus deferred infra components: database
migrations via CLI (idempotent, never edit applied ones), payments via
Stripe CLI, secrets via secrets CLI (never committed; owner-held keys → ⛔),
CI green on main (add minimal install/lint/build/test pipeline only if the
plan asks and none exists), .env.example accurate, README run steps verified
in a clean shell. If the plan and repo have none of these concerns, verify
and record that finding as the evidence. Branch chore/agent-92-infra.
ACCEPTANCE: every INFRA REQ dispositioned with CLI output / N/A + reason /
⛔ + exact action; no dashboard-only changes; no secrets in history; CI
green; README verified; PR merged.
COMMIT MESSAGE: agent-92-v1: infrastructure and config for Enhancement Plan
via CLI

================================================================================
      AGENT 93 — AI TOOLING DISCLOSURE (SINGLE-PURPOSE PR)
================================================================================
Insert into README.md, between "Acknowledgements" and "License" if those
exist, otherwise at the bottom, VERBATIM:

  ## AI tools used

  This project was developed with the assistance of Claude Code
  (Anthropic), used as a code-generation and pair-programming tool.

  The development pattern: I author the project plan, architecture
  decisions, design tokens, and review every change before it lands
  on a branch. The AI tooling generates the corresponding
  implementation under my instructions and commits on my behalf.

  All commits in this repository are authored under my git
  identity because I plan, direct, review, and accept
  responsibility for the work. This disclosure exists so that the
  use of AI tooling is part of the public record of the project.

Branch docs/agent-93-ai-disclosure; the PR contains ONLY the README change;
PR title EXACTLY: docs: add AI tooling disclosure
ACCEPTANCE: verbatim wording + placement; PR diff is README-only; exact
title; owner-authored commit with no trailers; PR merged.
COMMIT MESSAGE: docs: add AI tooling disclosure

================================================================================
    AGENT 94 — FINAL VERIFICATION + FULL REGRESSION + MERGE
================================================================================
Runs only when all other agents are ✅. 94.1 fresh clone to
C:\Users\Owner\dev\WebMARS-verify; install/build/run using ONLY documented
commands; record transcript. 94.2 matrix audit: every REQ IMPLEMENTED w/
merged PR or justified ⛔; five random Q1 spot-checks per category exercised
in the fresh clone. 94.3 full test suite on the fresh clone + manual
end-to-end walkthroughs of primary flows (changed and pre-existing);
regressions fixed via the loop. 94.4 ledger + appendix audit (no
placeholders anywhere). 94.5 git history audit: owner-authored, PR-merged,
zero AI identities/trailers, zero direct-to-main, zero force-pushes.
94.6 blocked-item report (owner-only items only). 94.7 final ledger note
(REQ total, PRs merged, build/test status, blocks); branch
chore/agent-94-final-verification; delete the verify clone.
ACCEPTANCE: all of the above evidenced; final PR merged. This is the ONLY
place stopping is permitted.
COMMIT MESSAGE: agent-94-v1: final verification — full Enhancement Plan
delivered on main

================================================================================
        APPENDIX A — PER-AGENT VERIFICATION LOG (FILL AS YOU GO)
================================================================================

--------------------------------------------------------------------------------
APPENDIX A-00 — Environment, Clone & Identity Pre-Flight
  STATUS: ✅ COMPLETE
  DATE: 2026-07-10
  TOOLCHAIN VERSIONS: git 2.54.0.windows.1 / gh 2.92.0 / node v24.14.0 /
    PowerShell 5.1.26100.8655
  gh auth status: logged in to github.com as BryanD17 (keyring), active,
    scopes gist/read:org/repo/workflow
  CLONE LOCATION + DEFAULT BRANCH: C:\Users\Owner\dev\WebMARS ; main
  git config user.name / user.email: "Bryan Djenabia" / "bdjenabia5@gmail.com"
  PDF: exists at C:\Users\Owner\Downloads\WebMARS-Enhancement-Plan.pdf,
    822,217 bytes, 60 pages, text extraction succeeded (pdfjs-dist, 106,609
    chars), copied to docs\WebMARS-Enhancement-Plan.pdf
  GROUND TRUTH: MIPS assembly simulator web app. Stack: React 19.2 + TS 6.0 +
    Vite 8 + Tailwind 4 + Zustand 5 + @monaco-editor/react 4.7. Build:
    `npm install` ; `npm run build` → exit 0 (bundle 417.64 kB / gzip 116.94
    kB). Tests: `npm test` → vitest, 14 files, 125/125 pass. Lint: `npm run
    lint` (eslint 10 flat config). Typecheck: `npm run typecheck`. CI:
    .github/workflows/ci.yml. Deploy: Vercel (vercel.json). Existing docs:
    PRD.md, STRETCH_ROADMAP.md, FINAL_REPORT.md, TASKS_LANDON.md,
    TASKS_ZACHARY.md, DEMO_SCRIPT.md, VERCEL_DEPLOY.md. Baseline npm audit:
    8 vulns (3L/2M/1H/2C) pre-existing.
  REGRESSIONS FIXED: n/a
  BLOCKED ITEMS: none
  PR # / MAIN COMMIT: PR #40 / main @ 4c9d661
--------------------------------------------------------------------------------
APPENDIX A-01 — PDF Ingestion & Requirements Extraction
  STATUS: ✅ COMPLETE
  DATE: 2026-07-10
  PAGES READ / TOTAL PAGES: 60 / 60 (text extraction via pdfjs-dist,
    106,609 chars; zero unreadable pages, no OCR needed)
  TOTAL REQs EXTRACTED: 48
  SECTIONS WITH NO ACTIONABLE CONTENT: §4 (team roles — dup of §6-8
    deliverables), §5.1 (slip decision tree — contingency), §10.5 (file
    summary — dup), §10.6 (reading order — onboarding prose). Recorded in
    the Appendix B section-coverage audit for A91 Q3 re-judgment.
  DYNAMIC AGENTS GENERATED: 11 (AGENT 02–12; titles in PROGRESS LEDGER)
  AMBIGUITIES INTERPRETED (G10 flags for owner override):
    REQ-001 [persistence mechanism: existing manual-key convention vs
      plan's persist middleware — behavioral acceptance binds];
    REQ-002 ['system' added to existing dark/light/hc set; hc keeps dark
      Monaco];
    REQ-014 [upstream rate limiter is hand-rolled, not Bucket4j — behavior
      verified rather than dependency mandated];
    REQ-017/019/044 [Render→Railway substitution already made by the team;
      plan's Render steps mapped to Railway equivalents];
    REQ-015 [tests authored+compiled; execution requires Docker absent on
      this machine — honest ⛔ on local execution].
  KEY SCOPING FACTS: see SCOPING FACTS SF1–SF6 section (backend mostly
    pre-implemented upstream; backend repo READ-only → fork PR path;
    backend live on Railway; frontend not yet integrated).
  REGRESSIONS FIXED: n/a
  BLOCKED ITEMS: none at this stage (owner-gated items surface in A12/A92)
  PR # / MAIN COMMIT: PR #41 (recorded before merge; squash hash in ledger
    note by AGENT 02's commit)
--------------------------------------------------------------------------------
APPENDIX A-02 — API Client, Toast System & Env-Aware Base URL
  STATUS: ✅ COMPLETE / DATE: 2026-07-10
  REQ-007: IMPLEMENTED — sonner added to package.json; <Toaster/> mounted
    in src/App.tsx (theme-aware: follows app theme, hc maps to dark;
    bottom-right, closeButton, richColors). Renders on both desktop and
    mobile shell paths (mounted beside <Shell/> at the App root).
  REQ-018: IMPLEMENTED — src/lib/api.ts:6 BASE = import.meta.env.VITE_API_BASE
    ?? 'http://localhost:8080'; .env.local.example at repo root documents
    local + production values (SF6).
  REQ-020: IMPLEMENTED — src/lib/api.ts: ApiError(status/body), authHeader()
    from webmars.token (privacy-mode safe try/catch), request<T> with JSON
    headers + 204 handling; register/login/getSnippet/getMySnippets/
    getPublicSnippets/saveSnippet/updateSnippet/deleteSnippet/logRun/
    getRecentRuns/getMostRunPrograms. Types corrected to the ACTUAL
    upstream serialization (owner={username} only, MostRun.snippetTitle,
    Spring Page envelope for /mine and /public) rather than the plan's
    guessed shapes. src/tests/api.test.ts: 5 tests (auth header on/off,
    ApiError status/body, JSON write bodies, 204 → undefined).
  BUILD+TEST: npm run typecheck → exit 0; npm run lint → exit 0; npm run
    build → exit 0 (bundle 450.46 kB / gzip 125.74 kB; +33 kB = sonner);
    npm test → 130/130 pass (15 files).
  PR # / MAIN COMMIT: PR #42.
--------------------------------------------------------------------------------
APPENDIX A-03 — Editor Persistence
  STATUS: ✅ COMPLETE / DATE: 2026-07-10
  REQ-001: IMPLEMENTED — src/hooks/useSimulator.ts: readPersistedWorkspace/
    writePersistedWorkspace (key webmars:workspace-v1, validated parse,
    privacy-mode-safe try/catch per existing convention); initialFiles/
    activeFileId/source hydrate from the persisted workspace; store
    subscription persists {files(id,name,source,modified), activeFileId}
    debounced 250ms + beforeunload flush. Simulator state excluded by
    construction — payload keys asserted to be exactly [activeFileId,
    files] in test. FileSystemFileHandle rehydrates as null. Mechanism
    follows the store's manual per-key convention, not persist middleware
    (G10 note in A-01) — behavioral acceptance identical.
    src/tests/persist.test.ts: 4 tests (debounced write, rehydration,
    no simulator state, corrupt-payload fallback).
  BUILD+TEST: typecheck 0 / lint 0 / build exit 0 / vitest 134/134 (16
    files). Live-browser refresh walk deferred to A91 Q1.
  PR # / MAIN COMMIT: PR #43.
--------------------------------------------------------------------------------
APPENDIX A-04 — Monaco Light Theme + System Preference
  STATUS: ✅ COMPLETE / DATE: 2026-07-10
  REQ-002: IMPLEMENTED —
    src/lib/mipsLanguage.ts: webmars-light theme defined beside
      webmars-dark (hex mirrors tokens.css [data-theme="light"]); both
      registered in registerMips.
    src/hooks/useSimulator.ts: ThemeName gains 'system'; THEMES + persisted
      validation accept it; resolveTheme(pref, systemScheme) exported.
    src/hooks/useSystemColorScheme.ts: NEW matchMedia hook with live
      change listener.
    src/ui/CodeEditor.tsx: theme prop derived (light → webmars-light;
      hc → webmars-dark per A-01 interpretation).
    src/ui/Shell.tsx: data-theme = resolveTheme(...), re-applies live on
      OS flips. src/App.tsx: Toaster follows resolved theme.
    src/ui/SettingsDialog.tsx: System radio option added; stale copy
      ("Editor stays on the dark Monaco theme") corrected.
  LIVE BROWSER VERIFICATION (playwright-core + Edge headless against
    npm run dev): Monaco .monaco-editor-background computed color
    rgb(10,10,10) in dark → rgb(255,255,255) after checking Light in
    SettingsDialog → rgb(10,10,10) after checking Dark again; data-theme
    ="light" applied; System resolved to host scheme. Screenshots
    captured (dark + light) — light shell renders with all 4 options.
  BUILD+TEST: typecheck 0 / lint 0 / build exit 0 / vitest 134/134.
  PR # / MAIN COMMIT: PR #44.
--------------------------------------------------------------------------------
APPENDIX A-05 — rem Pseudo-Instruction
  STATUS: / DATE: / PER-REQ (023): / BUILD+TEST: / PR #:
--------------------------------------------------------------------------------
APPENDIX A-06 — Tools: Bitmap + Magnifier
  STATUS: / DATE: / PER-REQ (006/028/029): / BUILD+TEST: / PR #:
--------------------------------------------------------------------------------
APPENDIX A-07 — Accounts UI
  STATUS: / DATE: / PER-REQ (021): / BUILD+TEST: / PR #:
--------------------------------------------------------------------------------
APPENDIX A-08 — Save, Share & Deep Links
  STATUS: / DATE: / PER-REQ (009/022/038): / BUILD+TEST: / PR #:
--------------------------------------------------------------------------------
APPENDIX A-09 — My Snippets + Public Feed
  STATUS: / DATE: / PER-REQ (032/033): / BUILD+TEST: / PR #:
--------------------------------------------------------------------------------
APPENDIX A-10 — Run History
  STATUS: / DATE: / PER-REQ (030/034): / BUILD+TEST: / PR #:
--------------------------------------------------------------------------------
APPENDIX A-11 — Polish, A11y, Docs, Tests, Issues
  STATUS: / DATE: / PER-REQ (031/035/036/037/039/040/042/046/047): /
  BUILD+TEST: / PR #:
--------------------------------------------------------------------------------
APPENDIX A-12 — Backend Verification + Gap PR
  STATUS: / DATE: / PER-REQ (003/004/005/010/011/012/013/014/015/016/024/
  025/026/027/041): / FORK PR URL: / BUILD: / BLOCKED: / PR #:
--------------------------------------------------------------------------------
(APPENDIX A blocks for agents 90–94 are filled as those agents complete,
using the A-90..A-94 field lists from the source master prompt.)

================================================================================
    APPENDIX B — REQUIREMENTS TRACEABILITY MATRIX (AGENT 01 POPULATES)
================================================================================

Statuses: ⬜ | 🔄 | IMPLEMENTED | ⛔ BLOCKED. IDs permanent. Interpretation
notes in [brackets] in the SUMMARY cell. A row flips to IMPLEMENTED only
when its owning agent's PR is MERGED and evidence is filled. ⛔ rows name
the exact owner action. At AGENT 94 exit: zero ⬜, zero 🔄.

  ID      | PDF PAGE/SECTION | SUMMARY | CATEGORY | OWNER | STATUS | PR # | EVIDENCE
  --------|------------------|---------|----------|-------|--------|------|---------
  REQ-001 | p.5-6 §2.1; p.32 §6.2 | Persist editor source/files/activeFileId across refresh; simulator state NOT persisted; private-mode safe; last-write-wins OK [mechanism per codebase convention, SF5] | FIX | A03 | IMPLEMENTED | #43 | useSimulator.ts workspace helpers + subscribe; persist.test.ts (4 tests)
  REQ-002 | p.6-7 §2.2; p.33-34 §6.3 | Monaco webmars-light theme; theme preference incl. 'system' w/ live OS tracking; SettingsDialog picker; shell+editor stay in sync [hc keeps dark Monaco] | FIX | A04 | IMPLEMENTED | #44 | mipsLanguage.ts webmars-light; browser-verified bg #0a0a0a→#ffffff; A-04
  REQ-003 | p.7-8 §2.3 | Backend: DELETE /snippets/{id} owner check, 404 to non-owners | SECURITY | A12 | ⬜ | | pre-exists upstream (verify)
  REQ-004 | p.8 §2.4 | Backend: login failures return 401 'Invalid credentials' (same msg both cases) | FIX | A12 | ⬜ | | pre-exists upstream (verify)
  REQ-005 | p.8-9 §2.5; p.38 §7.1; p.56 §10.1 | Backend secrets: env-var indirection, .env.example, .gitignore, rotation, git-history scrub + force-push + re-clone | SECURITY | A12 | ⬜ | | partial upstream; rotation/scrub = owner
  REQ-006 | p.9 §2.6; p.46-48 §8.2 | ScreenMagnifier rewrite: DOM-clone loupe 240x160 @2x, hint state, Escape, no per-mousemove reclone, Monaco excluded | FEATURE | A06 | ⬜ | |
  REQ-007 | p.10 §2.7; p.20 D1 | Toast system via sonner; Toaster at App root; error/success toasts for API calls | UI/UX | A02 | IMPLEMENTED | #42 | src/App.tsx (Toaster mount); A-02
  REQ-008 | p.10 §2.8 | Loading state on every async button: disabled + spinner/label while in flight; no duplicate submits | UI/UX | A90 | ⬜ | | implemented inline by A07-A10; A90 sweeps
  REQ-009 | p.10-11 §2.9; p.36 §6.6; p.23 D5 | ?snippet=<id> on boot loads snippet into editor; 404 → toast + default example; document title updated | FEATURE | A08 | ⬜ | |
  REQ-010 | p.11 §2.10; p.26 D8 | JWT expiry 8h (sprint choice) + README v1.3 note re refresh tokens | FIX | A12 | ⬜ | | 8h pre-exists (verify note)
  REQ-011 | p.11 §2.11; p.39 §7.2; p.56 §10.1 | CORS: prod + localhost origins, methods, headers, credentials, no wildcards | FIX | A12 | ⬜ | | pre-exists upstream (verify)
  REQ-012 | p.12-14 §3.2-3.4 | Flyway versioned migrations; final schema users/snippets(owner,visibility,updated_at,indexes)/runs(indexes) | INFRA | A12 | ⬜ | | V1-V4 pre-exist (verify contents)
  REQ-013 | p.12 §3.2; p.21 D2 | Request validation: spring-boot-starter-validation + Jakarta annotations on DTOs; JSON 400 field map | SECURITY | A12 | ⬜ | | pre-exists (verify)
  REQ-014 | p.12 §3.2; p.26 D8; p.56 §10.1 | Rate limiting /auth/**: 5 login/user/15min, 10 register/IP/hr, 429 + Retry-After [upstream hand-rolled, not Bucket4j — verify behavior] | SECURITY | A12 | ⬜ | | pre-exists (verify)
  REQ-015 | p.12 §3.2; p.49-51 §8.4; p.53 §9.1 | Testcontainers integration tests: PostgresTestBase + Auth/Snippet/Run/JwtUtil test classes per §9.1 scenario table | TEST | A12 | ⬜ | | MISSING upstream → fork PR; not executable locally (SF4)
  REQ-016 | p.13 §3.3 | Backend .env.example documents required vars; .gitignore excludes .env | INFRA | A12 | ⬜ | | pre-exists (verify)
  REQ-017 | p.15-16 §3.6.1; p.27-28 D10 | Backend deployed w/ env vars + Flyway migrations + JPA validate [Render→Railway substitution made by team] | INFRA | A92 | ⬜ | | live (verify + record)
  REQ-018 | p.16 §3.6.2; p.20 D1 | Frontend env-aware API base: VITE_API_BASE w/ localhost fallback; .env.local.example | INFRA | A02 | IMPLEMENTED | #42 | src/lib/api.ts:6; .env.local.example
  REQ-019 | p.16 §3.6.2; p.27 D10; p.56 §10.1 | Vercel env var VITE_API_BASE set + production redeploy + deployed-combo CORS check | INFRA | A92 | ⬜ | | dashboard = owner unless CLI authed
  REQ-020 | p.30-32 §6.1 | src/lib/api.ts typed client: ApiError, authHeader, request<T> w/ 204, full auth/snippets/runs surface per skeleton | FEATURE | A02 | IMPLEMENTED | #42 | src/lib/api.ts; src/tests/api.test.ts (5 tests)
  REQ-021 | p.34-35 §6.4; p.22 D4 | Auth slice (webmars.token/webmars.username) + AuthModal (tabs, busy, 401/409/429/network msg map) + toolbar Sign-in/username/Log-out | FEATURE | A07 | ⬜ | |
  REQ-022 | p.36 §6.5; p.23 D5 | Save-to-Account + ShareModal: create-vs-update semantics, copy link + toast, visibility shown + toggle → PUT | FEATURE | A08 | ⬜ | |
  REQ-023 | p.44-45 §7.7; p.22-24 D4-D5 | rem pseudo-instruction: lexer mnemonic, parser ["R","R","R"], assembler div+mfhi expansion | FEATURE | A05 | ⬜ | |
  REQ-024 | p.40 §7.3; p.24 D5 | Snippet entity v1.2: owner FK lazy, Visibility enum STRING, created/updated timestamps + lifecycle hooks | FEATURE | A12 | ⬜ | | pre-exists (verify)
  REQ-025 | p.40-42 §7.4; p.24 D6 | Run entity + RunRepository: findByUserIdOrderByStartedAtDesc + most-run JPQL projection | FEATURE | A12 | ⬜ | | pre-exists (verify)
  REQ-026 | p.42-43 §7.5; p.24 D6 | SnippetController v1.2: visibility-gated GET, /mine, /public paginated (size cap 100), /save sets owner, PUT update, DELETE owner-checked | FEATURE | A12 | ⬜ | | PUT missing upstream → fork PR
  REQ-027 | p.43-44 §7.6; p.25 D7 | RunController: POST /runs (user from JWT, never from body), GET /runs/recent, GET /runs/most-run, limit caps [+upstream audit C2: add visibility check on POST] | FEATURE | A12 | ⬜ | | pre-exists; C2 fix → fork PR
  REQ-028 | p.46 §8.1; p.20 D1 | BitmapDisplay DIMENSION_OPTIONS gains 8 and 16; existing sizes unaffected | FEATURE | A06 | ⬜ | |
  REQ-029 | p.47 §8.2; p.21 D2 | data-magnify-region on RegisterTable, MemoryPanel, ConsolePanel, StatusBar, FpuRegisterTable (+small-text panels); Monaco excluded | FEATURE | A06 | ⬜ | |
  REQ-030 | p.49 §8.3; p.25 D6 | Run-logging hook: fire-and-forget at run end (COMPLETED/ERROR/ABORTED), runStartedAt duration, skip unauth/unsaved, never block simulator | FEATURE | A10 | ⬜ | |
  REQ-031 | p.51-52 §8.5; p.53 §9.2 | Frontend tests: api.test.ts, persist.test.ts, rem assembler tests in existing harness | TEST | A11 | ⬜ | | authored by A02/A03/A05; A11 verifies completeness
  REQ-032 | p.24-25 D6 | MySnippetsDrawer: /mine list w/ title/updated/visibility badge, click-to-load, edit-in-place title, delete w/ confirm, New Snippet button, currentSnippetId persists | FEATURE | A09 | ⬜ | |
  REQ-033 | p.25 D7 | Public feed: paginated browser (title/owner/updated, Prev/Next/page) + 'See public snippets' discovery link in drawer empty state | FEATURE | A09 | ⬜ | |
  REQ-034 | p.26 D8 | HistoryPanel tab in right inspector: Recent Runs (color-coded status, duration, timestamp) + Most Run (count, last run); click loads snippet | FEATURE | A10 | ⬜ | |
  REQ-035 | p.26-27 D9 | Empty states w/ exact copy: drawer 'Save your first snippet…', history 'Run a saved snippet…', feed 'No public snippets yet. Be the first.' | UI/UX | A11 | ⬜ | |
  REQ-036 | p.27 D9 | Confirm dialogs for deletes: 'Delete <title>? This cannot be undone.' [Cancel][Delete] | UI/UX | A11 | ⬜ | |
  REQ-037 | p.27 D9 | AuthModal/ShareModal/confirm dialogs: focus trap, Escape closes, Enter submits | UI/UX | A11 | ⬜ | |
  REQ-038 | p.27 D9 | Browser back/forward between /?snippet=N and / restores snippet state (popstate) | FIX | A08 | ⬜ | |
  REQ-039 | p.27 D9 | New modals usable below 768px (mobile spot-check) | UI/UX | A11 | ⬜ | |
  REQ-040 | p.27 D9 | Frontend README rewritten for v1.2 surface: features, env vars, run + deploy instructions | DOCS | A11 | ⬜ | |
  REQ-041 | p.27 D9 | Backend README rewritten for v1.2: endpoints, env vars, deployment | DOCS | A12 | ⬜ | | pre-exists upstream (verify)
  REQ-042 | p.27 D9; p.24 D5 | HelpDialog: rem entry, magnifier docs (+which panels + Monaco limitation), bitmap 8x8/16x16, theme options note | DOCS | A11 | ⬜ | | contributions land in A04/A05/A06; A11 completes
  REQ-043 | p.27 D10; p.56 §10.1 | Pre-deploy gates: npm run lint / typecheck / test / build all clean on main; bundle-size review (no heavy accidental deps) | TEST | A92 | ⬜ | |
  REQ-044 | p.27-28 D10; p.56-57 §10.2 | Production deploy of integrated frontend + live smoke test (register→save→share→run→history on webmarsimulator.com) | INFRA | A92 | ⬜ | | Vercel deploy = owner unless CLI authed; smoke test follows
  REQ-045 | p.57 §10.3 | Risk mitigations w/ artifacts: secret-pattern scan (pre-commit/CI), README cold-start note, managed-Postgres expiry reminder note [Railway equivalent] | SECURITY | A92 | ⬜ | |
  REQ-046 | p.58 §10.4 | v1.3 backlog → GitHub issues tagged v1.3 (10 items: refresh tokens, email+reset, versioning, comments, forking, curation, profiles, snippet rate limits, GitHub OAuth, Monaco magnifier) | DOCS | A11 | ⬜ | |
  REQ-047 | p.28 D10; p.57 §10.2 | Demo video/gif + team announcement post | OTHER | A11 | ⬜ | | owner-only (cannot record video) → ⛔ expected
  REQ-048 | p.19 D-conventions; p.55 DoD | Sprint process: standups, human cross-review, branch/PR conventions | OTHER | A01 | ⬜ | | process prose; PR conventions honored by this run's workflow; human rituals N/A [annotation, no code]

  SECTION COVERAGE AUDIT (P1/1.4): §1 exec summary → dup of REQs (G9).
  §2 → REQ-001..011. §3 → REQ-012..019. §4 team roles → no actionable
  beyond dups (annotation). §5 day-by-day → REQ-001..048 dups + REQ-032/
  033/034/035..040/043/044/047/048 unique items captured. §6 → REQ-020/
  021/022/009/001/002. §7 → REQ-005/011/024/025/026/027/023. §8 → REQ-028/
  029/006/030/015/031. §9 → REQ-015/031 + manual QA checklist adopted as
  A91/A94 verification script (annotation). §10 → REQ-043/044/045/046/047
  + §10.5 file summary (dup annotation) + §10.6 reading order (no action).
  TOTAL: 48 REQs. sonner dep = REQ-007; '@playwright/test (optional)'
  p.59 = explicitly optional, skipped (G8 annotation, re-judged by A91).

================================================================================
        APPENDIX C — ENGINEERING CONVENTIONS IN FORCE FOR THIS RUN
================================================================================
C1 fully typed TS, no unjustified `any`. C2 intent-revealing names. C3 small
single-purpose functions. C4 errors handled at the actionable boundary,
never swallowed; user-facing failures actionable. C5 loading/empty/error are
part of the feature. C6 tokens over magic numbers; new tokens go to the
source. C7 semantic HTML first; keyboard-operable; AA contrast measured;
prefers-reduced-motion respected. C8 responsive verified at ~375/~768/≥1280;
touch targets ≥44px. C9 hot paths allocate nothing per frame; heavy assets
lazy. C10 changed behavior gets a test where a suite exists (it does:
vitest). C11 one agent = one branch = one PR; squash merges. C12 docs
updated in the same agent. C13 validate at boundaries; secrets via env; new
deps audited. C14 scope hygiene — nearby problems routed to AGENT 91, not
drive-by patched. Codebase conventions win over these where they conflict;
note the difference in Appendix A.

================================================================================
              APPENDIX D — ANTI-PATTERNS (INSTANT CRITERION FAILURES)
================================================================================
D1 checkbox on "looks right". D2 placeholder data on production paths.
D3 softening criteria. D4 direct push to main. D5 force-push to main.
D6 AI co-author trailers / --author / committer env vars. D7 "out of scope"
without owner sign-off. D8 fixing one instance and ignoring twins (Q2).
D9 editing applied migrations. D10 dashboard config with no evidence trail.
D11 silent scope creep. D12 template placeholders left in the committed doc.
D13 vague Appendix A prose. D14 stopping between agents to summarize/wait.

================================================================================
        APPENDIX E — GLOSSARY & CLI QUICK REFERENCE (PowerShell)
================================================================================
GOLDEN LOOP = the 10-step cycle. LEDGER = per-agent status board. REQ/MATRIX
= extracted requirement / Appendix B table. DYNAMIC AGENT = generated by
AGENT 01. EVIDENCE = file:line, command output, build result, or described
test. Q1/Q2/Q3 = hold? / other instances? / anything missed? ⛔ = owner-only
action with the exact step.
Identity check: git config user.name ; git config user.email
PR: gh pr create --base main --head <br> --title ... --body ... ;
gh pr merge --squash --delete-branch ; gh pr view <n> --json state,mergedAt
History audit: git log --format='%h | %an <%ae> | %cn <%ce> | %s'

================================================================================
      APPENDIX F — PR BODY TEMPLATE & EVIDENCE FORMATTING STANDARD
================================================================================
PR body: ## Agent (NN + loop position) / ## Requirements delivered (REQ list
+ files) / ## What changed and why (3–8 sentences) / ## Evidence summary
(build, tests, key verifications) / ## Out of scope / follow-ups (routed to
AGENT 91).
Evidence: E1 file:line + what it proves. E2 exact command + decisive output
line. E3 suite counts or described manual test. E4 what rendered, at which
breakpoint/state. E5 absence claims name where you looked. E6 every line
re-runnable by a skeptical reader.

================================================================================
    APPENDIX G — REQ EXTRACTION RUBRIC (FOR AGENT 01)
================================================================================
G1 imperatives are REQs. G2 desired-state statements are REQs. G3
qualitative wishes get measurable interpretations in [brackets]. G4 one REQ
per table row/list item unless trivially one change. G5 mockups are REQs
whose visible details are the acceptance checklist. G6 negative requirements
count (with Q2 sweep before deletion). G7 implied work extracted and marked
[implied]. G8 non-actionable prose → section annotated "no actionable
content" for Q3 re-judgment. G9 duplicates become ONE REQ with both pages.
G10 genuinely undecidable → more complete reading + both readings in the
note + flag in the ledger note.

================================================================================
    APPENDIX H — TROUBLESHOOTING KNOWN FAILURE MODES
================================================================================
H1 gh unauthenticated → surface "gh auth login", ⛔ merges, keep local
branches, never push to main. H2 branch protection → fix failing checks;
human-review requirement is an owner action (⛔, stack next agent's branch
on the unmerged head, record order). H3 merge conflict → merge origin/main
into the branch, preserve BOTH intents, rebuild, retest. H4 garbage PDF text
→ OCR/visual per page; ⛔ only after both fail. H5 CRLF churn → fix
.gitattributes/autocrlf in its own small commit, restage. H6 identity drift
→ full stop, --show-origin diagnosis, owner report; amend/reset-author only
on unpushed work; pushed history rewrite is owner-approved only. H7 baseline
red build → record; owning agent or AGENT 90 fixes. H8 dependency won't
install on Windows → documented Windows path, viable alternative recorded,
⛔ only if no route. H9 tempted to stop → re-read FINAL INSTRUCTION; D14.
H10 PR merged but local main stale → checkout main, pull, verify before
branching. H11 master-doc conflict → union both records, no status
regression, back to strict sequential. H12 scope larger than estimate →
split NN-a/NN-b in the ledger; splitting defers nothing.

================================================================================
  APPENDIX I — WORKED EXAMPLES: WHAT A GOOD RECORD LOOKS LIKE
================================================================================
Model ledger note: "[date] AGENT 03: <cluster> (REQ-009..013) done. PR #14
merged, main @ a1b2c3d. <one measurable proof>. <interpretation notes>. No
blocks." Model evidence line: "REQ-016: IMPLEMENTED — src/...:L40-L118 (new
pass), verified: <re-runnable check>. Build: npm run build → exit 0." Bad:
"done, looks much better now." Model ⛔ row names the exact owner command.
The bar: a stranger could resume — or audit — the run from these lines alone.

================================================================================

  FINAL INSTRUCTION — READ THIS EVERY TIME YOU FINISH AN AGENT

  Return to the GOLDEN LOOP. Open the PROGRESS LEDGER. Take the FIRST agent
  that is not ✅ COMPLETE and execute it fully, with evidence, then commit,
  push, open the PR, and MERGE TO MAIN. Then come back here and do it again.
  You stop when EVERY agent is ✅, AGENT 94 has passed, and every requirement
  on WebMARS-Enhancement-Plan.pdf is delivered on main with evidence, or
  ⛔ BLOCKED with the exact owner action.

  Do not stop until every agent is ✅.

================================================================================
